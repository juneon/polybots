# STATUS — 운영 로그 리뷰 & 진행 계획

> 작성: 2026-07-07 | 리팩토링 직후 상태. 구조/수정 내역은 `ANALYSIS.md`, 명세는 `SPEC.md` 참조.

---

## 1. 지금까지의 로그 정리

### 1.1 polybots_MA — 실거래 세션 (2026-03-03, 약 4시간)

MA breakout 전략의 유일한 라이브 기록. **137건 주문 (체결 122 / 거부 15)**

| 항목 | 값 |
|---|---|
| 기간 | 2026-03-03 11:56 ~ 15:56 (4h, 15 slugs 거래) |
| 매수 | 69건, 690토큰, 총 $199.96 (평균 체결가 0.290) |
| 매도 | 53건, 653.5토큰, 총 $172.66 (평균 체결가 0.272) |
| **실현 손익** | **−$27.30** (회전액 대비 약 −13.7%) |
| slug 승률 | **3승 12패** (최고 +$3.01 / 최악 −$7.19) |
| 청산 구성 | exit_time(MA 이탈) 52건 +$162.90, exit_tp 1건 +$9.76 |
| 미청산 | 매수−매도 = **36.5토큰이 청산 실패로 만기 노출** |

거부 15건의 내역:
- `sell_dust:below_step` × 14 (전부 exit_time) — 매도 스윕이 잔고를 못 찾음(잔고 반영 지연/이미 dust). **미청산 36.5토큰의 직접 원인.**
- FAK no-match × 1 (**exit_tp**) — TP 순간 유동성 없음 → 익절 실패 사례.

### 1.2 polybots_pre — 실거래 기록 (2026-03-02)

단 1회 왕복: buy up @0.81 (10토큰) → exit_sl @0.74 (9.9토큰) = **−$0.74**. 표본 부족으로 평가 불가.

### 1.3 백테스트 vs 라이브 갭 ⚠️ (가장 중요한 발견)

| | 백테스트 (7일) | 라이브 (4시간) |
|---|---|---|
| PnL | **+$81.66** (505 trades) | **−$27.30** (122 fills) |
| 가정 | 호가 그대로 100% 체결, 슬리피지/수수료 0 | market 모드 = bid−0.05 매도, 잔고 지연, 체결 실패 |

라이브 손실의 구조적 요인 후보:
1. **슬리피지 이중 부담**: market 매도가 `bid − slippage(0.05)`로 나감 — 토큰당 5센트면 10토큰 왕복에 $0.5, 69 왕복이면 그것만 ~$34.
2. 평균 매수 0.290 vs 평균 매도 0.272 = 왕복당 −1.8센트/토큰.
3. 청산 실패(위 14건)로 만기 소멸분.
→ **백테스트에 슬리피지/체결 모델을 넣기 전까지 그리드 서치 결과를 신뢰하면 안 됨.**

### 1.4 보유 데이터 자산

| 데이터 | 규모 | 용도 |
|---|---|---|
| `backtest/data/*_events.csv` (7일: 260126~260227) | ~80MB | 그리드 서치 입력 |
| `polybots_MA/logs/events.csv` (26-03-03) | 8.3MB, 14.4k quotes, 18 slugs | 백테스트 추가 가능 (아직 미사용) |
| `polybots_pre/logs/events.csv` | 0.2MB, 1 slug | 무시 가능 |
| 신규 `logs/` (run_id별 append) | 수집 중 | 리팩토링 후 검증 + 백테스트 소스 |

### 1.5 신규 모노레포 sim 가동 (2026-07-07~)

- 스모크: `20260706_235841_ma_breakout_sim` — 진입 1건 체결 확인 (buy up @0.14, tick 17).
- **현재 rolling sim 가동 중**: `20260707_000329_ma_breakout_sim` — MA(300틱) 워밍업 후 연속 수집.
- 확인 방법: `logs/trades.csv`·`snapshots.csv`를 run_id로 필터. 중지: 실행 중인 python 프로세스 종료.

## 2. 추후 진행 계획

### P0 — sim 검증 기간 (지금, 진행 중)
- [ ] rolling sim을 최소 1~2일 가동, slug 20개 이상에서 진입/청산/slug 전환/로그 무결성 확인
- [ ] sim 결과 vs 같은 기간 시세로 수동 검산 (전략 로직 회귀 확인)

### P1 — 백테스트 현실화 (live 재투입 전 필수)
- [ ] backtest에 **슬리피지/체결 모델** 추가 (market 매도 = bid − slippage, 매도 실패 확률)
- [ ] 3-03 라이브 events.csv(8.3MB)를 데이터에 편입해 재실행
- [ ] 현실화된 그리드로 파라미터 재선정 — 현행 cap=0.5/ma=300은 무비용 가정의 최적값
- [ ] backtest가 `strategies/ma_breakout.py`를 직접 리플레이하도록 통합 (전략-백테스트 코드 이중화 제거)

### P2 — 실행 품질 (라이브 손실 요인 제거)
- [ ] `sell_dust:below_step` 대응: 스윕 창 연장/재시도 정책, 청산 실패 시 다음 tick 재발행은 리팩토링으로 반영됨 — 라이브 검증 필요
- [ ] exit_tp를 limit 유지가 아니라 잔존 유동성 고려(FAK no-match 사례) — TP도 스윕 경로로
- [ ] 매도 스윕의 메인 루프 블로킹(≤10초) → 스레드 분리
- [ ] 주문 전 USDC 실잔고 가드

### P3 — 인프라
- [ ] tick당 HTTP 4회 순차 → 병렬화 또는 CLOB WebSocket
- [ ] slug 경계 404 폴백 (로컬 시계 레이스)
- [ ] 서버 상시 가동(ver3), 로그 로테이션

### P4 — 확장 (SPEC ver4/ver5)
- [ ] ETH/SOL/XRP: `configs/`에 slug prefix만 다른 config 추가로 대응 가능
- [ ] Binance ATR 전략: `core/adapters_binance.py` + `strategies/atr_*.py`
- [ ] 레거시 폴더 3개: 새 구조 안정화 확인 후 정리

### 재투입 기준 (제안)
sim에서 slug 30개 이상 + P1 백테스트 현실화 후에도 기대값이 양수일 때만 live 소액 재개.
