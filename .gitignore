# secrets — NEVER commit
.env

# python
__pycache__/
*.pyc
.pytest_cache/

# IDE-generated mirror of CLAUDE.md — never commit
AGENTS.md

# volatile runtime — machine-local (heartbeat/stop files/stdout logs)
logs/ctl/

# stray pre-migration sim accounts at root (current state lives in state/, tracked)
/sim_account*.json

# regenerable artifacts (rebuilt by backtest/data_prep.py)
backtest/data/quotes_all.parquet

# local-only
configs/backups/
.claude/settings.local.json

# legacy checkouts (own git repos, kept as archive)
archive/
