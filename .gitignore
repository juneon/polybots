# secrets — NEVER commit
.env

# python
__pycache__/
*.pyc
.pytest_cache/

# IDE-generated mirror of CLAUDE.md — never commit
AGENTS.md

# runtime artifacts
logs/
state/
sim_account*.json
backtest/results/

# backtest raw data (large; regenerated from live runs' events.csv)
backtest/data/

# legacy checkouts (own git repos, kept as archive)
archive/

# local-only claude settings
.claude/settings.local.json
configs/backups/
